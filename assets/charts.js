// Generated by Trae Work
(function() {
  var style = getComputedStyle(document.documentElement);
  var accent = style.getPropertyValue('--accent').trim();
  var accent2 = style.getPropertyValue('--accent2').trim();
  var accent3 = style.getPropertyValue('--accent3').trim();
  var ink = style.getPropertyValue('--ink').trim();
  var muted = style.getPropertyValue('--muted').trim();
  var rule = style.getPropertyValue('--rule').trim();
  var bg2 = style.getPropertyValue('--bg2').trim();
  var bg3 = style.getPropertyValue('--bg3').trim();
  var success = style.getPropertyValue('--success').trim();
  var warning = style.getPropertyValue('--warning').trim();

  // --- Radar Chart: Algorithm Comparison ---
  var radarChart = echarts.init(document.getElementById('chart-radar'), null, { renderer: 'svg' });
  radarChart.setOption({
    backgroundColor: 'transparent',
    tooltip: {
      trigger: 'item',
      appendToBody: true,
      backgroundColor: bg2,
      borderColor: rule,
      textStyle: { color: ink }
    },
    legend: {
      data: ['KNN', '线性回归', '逻辑回归', '决策树', '随机森林', 'SVM', '朴素贝叶斯', 'K-Means'],
      bottom: 0,
      textStyle: { color: muted, fontSize: 12 },
      itemWidth: 12,
      itemHeight: 12,
      itemGap: 15
    },
    radar: {
      indicator: [
        { name: '准确率', max: 100 },
        { name: '训练速度', max: 100 },
        { name: '预测速度', max: 100 },
        { name: '可解释性', max: 100 },
        { name: '抗过拟合', max: 100 },
        { name: '数据缩放敏感', max: 100 },
        { name: '高维数据表现', max: 100 }
      ],
      shape: 'polygon',
      splitNumber: 5,
      axisName: {
        color: ink,
        fontSize: 13,
        fontWeight: 500
      },
      splitLine: {
        lineStyle: { color: rule }
      },
      splitArea: {
        show: true,
        areaStyle: {
          color: ['rgba(56,189,248,0.02)', 'rgba(56,189,248,0.05)']
        }
      },
      axisLine: {
        lineStyle: { color: rule }
      }
    },
    series: [{
      type: 'radar',
      symbolSize: 6,
      lineStyle: { width: 2 },
      data: [
        {
          value: [65, 95, 40, 85, 50, 100, 45],
          name: 'KNN',
          lineStyle: { color: accent },
          itemStyle: { color: accent },
          areaStyle: { color: accent + '20' }
        },
        {
          value: [60, 90, 95, 95, 55, 70, 40],
          name: '线性回归',
          lineStyle: { color: accent2 },
          itemStyle: { color: accent2 },
          areaStyle: { color: accent2 + '20' }
        },
        {
          value: [70, 85, 95, 80, 60, 80, 60],
          name: '逻辑回归',
          lineStyle: { color: accent3 },
          itemStyle: { color: accent3 },
          areaStyle: { color: accent3 + '20' }
        },
        {
          value: [75, 85, 90, 90, 35, 20, 50],
          name: '决策树',
          lineStyle: { color: success },
          itemStyle: { color: success },
          areaStyle: { color: success + '20' }
        },
        {
          value: [90, 60, 75, 60, 85, 30, 70],
          name: '随机森林',
          lineStyle: { color: warning },
          itemStyle: { color: warning },
          areaStyle: { color: warning + '20' }
        },
        {
          value: [85, 40, 65, 30, 75, 95, 90],
          name: 'SVM',
          lineStyle: { color: '#f472b6' },
          itemStyle: { color: '#f472b6' },
          areaStyle: { color: '#f472b620' }
        },
        {
          value: [70, 95, 95, 65, 65, 60, 75],
          name: '朴素贝叶斯',
          lineStyle: { color: '#22d3ee' },
          itemStyle: { color: '#22d3ee' },
          areaStyle: { color: '#22d3ee20' }
        },
        {
          value: [60, 80, 85, 70, 45, 90, 50],
          name: 'K-Means',
          lineStyle: { color: '#a78bfa' },
          itemStyle: { color: '#a78bfa' },
          areaStyle: { color: '#a78bfa20' }
        }
      ]
    }]
  });

  window.addEventListener('resize', function() {
    radarChart.resize();
  });
})();
